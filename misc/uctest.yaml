name: uctest
id: http://example.com/uctest.yaml
prefixes:
  linkml: https://w3id.org/linkml/
imports:
  - linkml:types
slots:
  uc:
    range: uriorcurie
  name:
    range: string
classes:
  testclass:
    slots:
      - uc
      - name
    see_also:
      - relative_file_url.txt

# poetry run linkml-convert --output ucdata.json --target-class testclass --schema uctest.yaml ucdata.yaml